package com.theminequest.common.quest.js;

public class JsQuestRequirementFunctions {
	
	/*
	 * There's not much to say here. It's very simple to invocate:
	 * 
	 * bool = requirement.check(reqID)
	 * 
	 * If you want to dynamically create one on the fly:
	 * 
	 * reqID = requirement.create(name, details, varargs, etc)
	 * bool = requirement.check(reqID)
	 */
	
}
